{{- defineDatasource "deployment" "env:///DEPLOYMENT?type=application/json" -}}
{{- $deployment := ds "deployment" -}}
{{- printf "CF_DNS_API_TOKEN={{ %s }}\n" $deployment.server.runtime.credentials.cloudflare_acme_token -}}
{{- printf "MONITORING_TOKEN={{ %s }}\n" $deployment.service.runtime.credentials.monitoring_token -}}
